# Copy to .env.local and set your live Railway public URL
# Railway → Project → Service → Settings → Networking → Public domain
VITE_API_URL=https://your-service.up.railway.app
VITE_BACKEND_URL=https://your-service.up.railway.app
