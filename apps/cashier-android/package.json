{
  "name": "softshape-cashier-android",
  "version": "23.12.31",
  "description": "SoftShape Cashier Android App (Capacitor)",
  "private": true,
  "type": "module",
  "scripts": {
    "build:web": "cd ../.. && npx vite build --mode cashier-android",
    "sync": "npx cap sync android && node native/sync-native.mjs",
    "build": "npm run build:web && npm run sync",
    "open": "npx cap open android",
    "apk": "npm run build:web && npm run sync && npx cap build android --androidreleasetype APK"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/app": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/haptics": "^7.0.0",
    "@capacitor/keyboard": "^7.0.0",
    "@capacitor/status-bar": "^7.0.0",
    "@capacitor-firebase/authentication": "^8.3.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
